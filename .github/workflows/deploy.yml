# 原始文件内容（完整保留并注释，原文未删减）——以注释形式保存在文件顶部：
# name: Deploy Personal Tech Card
# on:
#  push:
#  branches:
#  - main # 只在main分⽀更新时触发
# jobs:
#  build-and-deploy:
# 步骤9.3：提交CI配置
# 10. 配置GitHub Pages【必做】
# 步骤10.1：启⽤GitHub Pages
# 1. 进⼊仓库的 Settings ⻚⾯
# 2. 在左侧菜单找到 Pages
# 3. 在 Build and deployment 部分：
# Source: 选择 GitHub Actions
#  runs-on: ubuntu-latest
#  # 为GitHub Pages设置权限
#  permissions:
#  contents: read
#  pages: write
#  id-token: write
#  steps:
#  - name: Checkout code
#  uses: actions/checkout@v4
#  - name: Set up Python
#  
#  
#  
# uses: actions/setup-python@v5
# with:
# python-version: '3.10'
#  - name: Generate HTML file
#  run: python build_page.py
#  - name: Setup Pages
#  uses: actions/configure-pages@v4
#  - name: Upload artifact
#  
#  
#  
# uses: actions/upload-pages-artifact@v3
# with:
# path: '.' # 上传整个⽬录，GitHub Pages会找到index.html
#  - name: Deploy to GitHub Pages
#  
#  
# id: deployment
# uses: actions/deploy-pages@v4

name: Deploy Personal Tech Card
on:
  push:
    branches:
      - main # 只在main分支更新时触发
jobs:
  build-and-deploy:
    # 步骤9.3：提交CI配置
    # 10. 配置GitHub Pages【必做】
    # 步骤10.1：启用 GitHub Pages
    # 1. 进入仓库的 Settings 页面
    # 2. 在左侧菜单找到 Pages
    # 3. 在 Build and deployment 部分：
    # Source: 选择 GitHub Actions
    runs-on: ubuntu-latest
    # 为 GitHub Pages 设置权限
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Generate HTML file
        run: python build_page.py
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.' # 上传整个目录，GitHub Pages 会找到 index.html
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4